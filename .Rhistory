summarize_coil <- coil_data %>% group_by(manufacturing_lot) %>% summarize(mean_psi=mean(psi), .groups = 'keep')
install.packages(tidyverse)
library(tidyverse)
summarize_coil <- coil_data %>% group_by(manufacturing_lot) %>% summarize(mean_psi=mean(psi), .groups = 'keep')
coil_data <- read.csv(file = 'suspension_coil.csv',check.names = F, stringsAsFactors = F)
setwd("C:/Users/brend/Desktop/Bootcamp Modules/Module 15/MechaCar_Analysis")
coil_data <- read.csv(file = 'suspension_coil.csv',check.names = F, stringsAsFactors = F)
summarize_coil <- coil_data %>% group_by(manufacturing_lot) %>% summarize(mean_psi=mean(psi), .groups = 'keep')
summarize_coil <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(mean_psi=mean(psi), .groups = 'keep')
summarize_coil <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(mean_psi=mean(PSI), .groups = 'keep')
View(summarize_coil)
summarize_coil <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(Mean=mean(PSI), Median=median(PSI), Variance=variance(PSI), SD=sd(PSI) .groups = 'keep')
summarize_coil <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(Mean=mean(PSI), Median=median(PSI), Variance=variance(PSI), SD=sd(PSI), .groups = 'keep')
summarize_coil <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(Mean=mean(PSI), Median=median(PSI), Variance=var(PSI), SD=sd(PSI), .groups = 'keep')
View(summarize_coil)
lot_summary <- coil_data %>% group_by(Manufacturing_Lot) %>% summarize(Mean=mean(PSI), Median=median(PSI), Variance=var(PSI), SD=sd(PSI), .groups = 'keep')
View(lot_summary)
coil_data %>%
summarize(Mean=mean(PSI), Median=median(PSI), Variance=var(PSI), SD=sd(PSI))
total_summary <- coil_data %>%
summarize(Mean=mean(PSI), Median=median(PSI), Variance=var(PSI), SD=sd(PSI))
View(total_summary)
